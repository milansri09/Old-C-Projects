#include <stdio.h>
//#include <graphics.h>
#include <allegro5/allegro5.h>

int main(void) {
    //printf("Testing, testing");

    // initwindow(600,600,"Snake Game");
    // circle(0,0,30);
    
    // getch();
    // closegraph();

    al_init();
    al_install_keyboard();
    
    
    return 0;
}